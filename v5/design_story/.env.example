# ANTHROPIC_API_KEY is not used by this app.
# Authentication is handled by the `claude` CLI invoked as a subprocess;
# it reads your Claude subscription OAuth credentials from the user keychain
# (run `claude auth login` once).
#
# The server explicitly unsets ANTHROPIC_API_KEY before spawning `claude`
# to guarantee subscription usage instead of API billing.
