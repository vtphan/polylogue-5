# Simplified Schemas

This directory contains human-readable schema sketches for the simplified artifacts:

- `story.yaml`
- `episode-plan.yaml`
- `transcript.yaml`
- `lesson_package.yaml`

They are documentation aids. The validators in `pipeline/scripts/` are the canonical structural checks.

