import { SessionEntryPage } from "@/features/session/components/session-entry-page";

export default function Home() {
  return (
    <SessionEntryPage
      episodeContext="A group of students at one table will read a short Polylogue episode, choose one focal turn together, and build a response in sequence."
      episodeTitle="Strangers in the Old Forest"
    />
  );
}
